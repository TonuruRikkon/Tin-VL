#include<bits/stdc++.h>
using namespace std;

int maxlengthlis(int i,vector<int> inp, vector<int> dp){
    if(i==0) return 1;
    
}

int lis(int n, vector<int> inp){
    vector<int> dp(n,-1);
    int ans=1;
    for (int i = 0; i < n; i++)
    {
        ans = max(ans, )
    }
    
}

int main()
{
    int n;
    cin>>n;
    vector<int> inp;
    for (int i = 0; i < n; i++)
    {
        int x;
        cin>>x;
        inp.emplace_back(x);
    }
    
}