#include <bits/stdc++.h>
using namespace std;

int main()
{
    int m,n,q;
    vector<vector<int>> inp((m+1),vector<int>(n+1));
    cin>>m>>n>>q;
    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < n; j++)
        {
            cin>>inp[i][j];
        }
        
    }
    
}