#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    vector<int> score;
    vector<int> tol_score;
    vector<string> name_pos;
    vector<string> name;
    for (int i = 0; i < n; i++)
    {
        string x;
        int y;
        cin>>x>>y;
        name.push_back(x);
        name_pos.push_back(x);
        score.push_back(y);
    }
    
}