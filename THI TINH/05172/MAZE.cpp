#include<bits/stdc++.h>
using namespace std;
vector<int>a;
int main()
{
    freopen("MAZE.INP","r",stdin);
    freopen("MAZE.OUT","w",stdout);
    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            cin>>a[i];
            cin>>a[j];
        }
    }
}
